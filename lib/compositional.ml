
(* Compositionally analyzes a set of CFGs *)
(* TODO: inspired by inter-fixpoint, but only need one pass over each functionn? (except maybe for mutually recursive ones? *)
(* This is actually all done in interfixpoint currently *)
      

  
  
